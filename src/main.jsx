import React from "react";
import ReactDOM from "react-dom/client";
import GolfLeagueApp from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GolfLeagueApp />
  </React.StrictMode>
);
